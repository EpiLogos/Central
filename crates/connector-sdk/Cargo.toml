[package]
name = "central-connector-sdk"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "central_connector_sdk"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
