[package]
name = "central-harness-connector"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "central_harness_connector"
path = "src/lib.rs"

[dependencies]
central-connector-sdk = { path = "../../crates/connector-sdk" }
serde_json = "1"

[dev-dependencies]
central-reference-connectors = { path = "../reference" }
